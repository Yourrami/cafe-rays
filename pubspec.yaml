name: cafe_rays
description: Café Rays - تطبيق إدارة المبيعات اليومية
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.1
  intl: ^0.18.1
  pdf: ^3.10.6
  printing: ^5.11.0
  share_plus: ^7.2.1
  provider: ^6.1.1
  google_fonts: ^6.1.0
  flutter_reorderable_list: ^1.0.0
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
